{
  "extends": "@repo/typescript-config/base.json",
  "compilerOptions": {
    "experimentalDecorators": true, // Required for WatermelonDB
    "emitDecoratorMetadata": true,
    "strict": false // Loosening strictness slightly for WD types, or do I need it? 
    // Actually, WD should work with strict true but needs proper typing
  },
  "include": [
    "src/**/*"
  ]
}